<fieldset>
  <legend>Creating album</legend>
  <%= form_for @album, :html => { :multipart => true } do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>  
</fieldset>
 
<%= link_to 'Show', albums_show_path %> 

