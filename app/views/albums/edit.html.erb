<h1>Editing album</h1>
 
<%= form_for @album, :html => { :multipart => true } do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
 
<%= link_to 'Show', @album %> |
<%= link_to 'Back', albums_path %>

