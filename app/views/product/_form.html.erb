<% form_for @product, :html => { :multipart => true } do |f| %>
  <p>
    <%= f.label :name %>
  </p>
  <p>
    <%= f.label :description %>
  </p>
  <p>
    <%= f.file_field :photo %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
