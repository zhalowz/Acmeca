<h1>Gallery</h1>
  <% for album in @albums %>
    <table>
    <tr>
       <td><%= link_to album.name, albums_view_path %></td>
    </tr>
 
    <tr>
    <td><%= image_tag album.photos.first.data.url, :alt => "" %></td>
    </tr>
      </table>
  <% end %>


