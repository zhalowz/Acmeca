<h1>Gallery Display</h1>
<div class="albums">
<% for album in @albums %>
  <p class="album">
    <%= link_to album.name, manage_album_path(album) %>
    <%= get_first_image(album, :small) %>
  </p>
<% end %>
</div>
<%= link_to 'Back', manage_index_path %>
