<%= form_for @album, :url => manage_album_path(@album), :html => { :multipart => true } do |f| %>
  <fieldset>
    <legend>Editing album</legend>
    <%= render :partial => 'form', :locals => {:f => f} %>
  </fieldset>
<% end %>
<%= link_to 'Back', manage_album_path(@album) %> 

