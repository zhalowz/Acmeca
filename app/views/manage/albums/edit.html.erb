<fieldset>
  <legend>Editing album</legend>
  <%= form_for @album, :html => { :multipart => true } do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
  <% end %>
</fieldset>
<%= link_to 'Back', @album %> 

