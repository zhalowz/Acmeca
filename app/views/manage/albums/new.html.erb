<%= form_for @album, :url => { :action => :create }, :html => { :multipart => true } do |f| %>
  <fieldset>
    <legend>Creating album</legend>
      <%= render :partial => 'form', :locals => {:f => f} %>
  </fieldset>
<% end %>  
<%= link_to 'View All', manage_albums_path %> 

