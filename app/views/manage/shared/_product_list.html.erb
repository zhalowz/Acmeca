Categories: 
<%= link_to "All", manage_products_path %>
<% for category in @categories %>
<%= link_to category.name, manage_category_path(category) %>
<% end %>
<%= will_paginate @products %>
<div class="catalog2">
<% for product in @products %>
  <div class="catalog2-item">

    <%= link_to image_tag(product.photo.url(:small)), manage_product_path(product), { :class => "catalog2-img"}  %>
    <div class="details">
      <%= link_to product.name, manage_product_path(product) %>
      <br />
      <%= sanitize(redcloth(product.description)) %>
    </div>

  </div>
<% end %>
</div>
<%= will_paginate @products %>
<p class="actions"><%= link_to "Back to Manage", manage_index_path %></p>
 

