<%= form_for @service, :url => manage_service_path(@service), :html => { :multipart => true } do |f| %>
<fieldset>
  <legend>Edit services</legend>
  <%= render :partial => "form", :locals => { :f => f } %>
</fieldset>
<% end %>
<%= link_to 'Back', manage_services_path %>
