<%= form_for @sale, :url => { :action => :create }, :html => { :multipart => true } do |f| %>
  <fieldset>
    <legend>Create Sale</legend>
    <%= render :partial => "form", :locals => { :f => f } %>
  </fieldset>
<% end %>
<%= link_to 'Back', manage_index_path %> 
