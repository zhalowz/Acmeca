<%= form_for @installation, :url => { :action => :create }, :html => { :multipart => true } do |f| %>
  <fieldset>
    <legend>New Installation</legend>
      <%= render :partial => 'form', :locals => {:f => f} %>
  </fieldset>
<% end %>  
<%= link_to 'View All', manage_installations_path %> 

