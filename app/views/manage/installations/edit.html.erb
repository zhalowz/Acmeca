<%= form_for @installation, :url => manage_installation_path(@installation), :html => { :multipart => true } do |f| %>
  <fieldset>
    <legend>Editing Installation</legend>
    <%= render :partial => 'form', :locals => {:f => f} %>
  </fieldset>
<% end %>
<%= link_to 'Back', manage_installation_path %> 

