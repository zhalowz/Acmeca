<%= form_for @product, :url => manage_product_path(@product), :html => { :multipart => true } do |f| %>
<fieldset>
  <legend>Edit product</legend>
  <%= render :partial => "form", :locals => { :f => f } %>
</fieldset>
<% end %>
